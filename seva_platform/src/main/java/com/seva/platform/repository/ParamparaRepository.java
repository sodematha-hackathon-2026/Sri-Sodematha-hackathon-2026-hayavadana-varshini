package com.seva.platform.repository;

import com.seva.platform.model.Parampara;
import org.springframework.data.jpa.repository.JpaRepository;

public interface ParamparaRepository extends JpaRepository<Parampara, Long> {
}
