package com.seva.platform.repository;

import com.seva.platform.model.Miracle;
import org.springframework.data.jpa.repository.JpaRepository;

public interface MiracleRepository extends JpaRepository<Miracle, Long> {
}
